@using E_Matura.Models.ViewModels.Questions
@model E_Matura.Models.ViewModels.Matura.MaturaVm
@{
    ViewBag.Title = "Generate12BgMatura";
}

<h2>Generate12BgMatura</h2>
@for (int i = 0; i < Model.Questions.Count; i++)
{
    if (Model.Questions[i] is QuestionClosedAnswerTestVm)
    {
        @RenderPage("_ClosedAnswerPartial.cshtml", Model.Questions[i])
    }
}
